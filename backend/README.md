# HITS Backend
## How to Setup
Run `uv sync` to initialize and download dependencies using `uv`\
## How to Run
in the main folder run the command:\
`uv run fastapi dev main.py --host 0.0.0.0`
## How to use in an app
use `ifconfig` on linux to get the address of the backend device and put that in frontend apps environment collection